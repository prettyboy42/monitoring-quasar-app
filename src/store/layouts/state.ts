export interface LayoutState {
  leftDrawerOpen: boolean;
  rightDrawerOpen: boolean;
  headerState: boolean;
  footerState: boolean;
  forceRefresh: boolean;
  refreshTimeInterval: number;
  tickTimeInterval: number;
  timeRangeInterval: number;
}
